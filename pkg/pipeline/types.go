package pipeline

import "github.com/aula-id/etl-pipeline-go/pkg/model"

type (
	Record          = model.Record
	RecordBatch     = model.RecordBatch
	CheckpointToken = model.CheckpointToken
	Source          = model.Source
	Sink            = model.Sink
)
